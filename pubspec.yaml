name: erp_system
description: A modular ERP system for sales, inventory, finance, and more.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.2
  get_it: ^7.6.0
  dio: ^5.3.2
  equatable: ^2.0.5
  json_annotation: ^4.8.1
  responsive_framework: ^1.0.0
  provider: ^6.0.5

dev_dependencies:
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
